import SwiftUI
import AppShell

/// Plizz — an open-source tvOS Jellyfin client.
@main
struct PlizzApp: App {
    var body: some Scene {
        WindowGroup {
            RootView()
        }
    }
}
