import SwiftUI
import AppShell

/// Plozz — an open-source tvOS Jellyfin client.
@main
struct PlozzApp: App {
    var body: some Scene {
        WindowGroup {
            RootView()
        }
    }
}
